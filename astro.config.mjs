// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    site: 'https://soulef.github.io',
    // Si le repo ne s'appelle PAS votre-username.github.io :
    // base: '/nom-du-repo',
})
